name: Docs

on:
    push:
        branches:
            - main
        paths:
            - 'apps/notes/docs/**'
    pull_request:
        branches:
            - main
        paths:
            - 'apps/notes/docs/**'

permissions:
    contents: write

concurrency:
    group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
    cancel-in-progress: true

jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
            - name: Checkout [Pull Request]
              uses: actions/checkout@v3
              if: github.event_name == 'pull_request'
              with:
                  ref: ${{ github.event.pull_request.head.sha }}
                  fetch-depth: 0

            - name: Derive SHAs
              uses: nrwl/nx-set-shas@v3
              if: github.event_name == 'pull_request'

            - name: Install Node
              uses: actions/setup-node@v3
              if: github.event_name == 'pull_request'
              with:
                  node-version: 16

            - name: Cache Node
              id: node_cache
              uses: actions/cache@v3
              if: github.event_name == 'pull_request'
              with:
                  path: |
                      dist/**/.next/cache
                      node_modules/
                  key: ubuntu-latest-node-${{ hashFiles('yarn.lock') }}
                  restore-keys: ubuntu-latest-node-

            - name: Install dependencies
              if: github.event_name == 'pull_request' && steps.node_cache.outputs.cache-hit != 'true'
              run: yarn

            - name: Build Documentation
              if: github.event_name == 'pull_request'
              run: yarn nx build docs --skip-nx-cache

            - name: Cache Docs
              id: docs_cache
              uses: actions/cache@v3
              if: github.event_name == 'pull_request'
              with:
                  path: |
                      dist/apps/notes/docs
                  key: ubuntu-latest-docs-${{ hashFiles('apps/notes/docs/**/*') }}
                  restore-keys: ubuntu-latest-docs-

    deploy:
        name: Deploy
        needs: [build]
        runs-on: ubuntu-latest
        steps:
            - name: Checkout [Main]
              uses: actions/checkout@v3
              if: github.event_name != 'pull_request'
              with:
                  fetch-depth: 0

            - name: Install Node
              uses: actions/setup-node@v3
              if: github.event_name != 'pull_request'
              with:
                  node-version: 16

            - name: Cache Node
              id: node_cache
              uses: actions/cache@v3
              if: github.event_name != 'pull_request'
              with:
                  path: |
                      dist/**/.next/cache
                      node_modules/
                  key: ubuntu-latest-node-${{ hashFiles('yarn.lock') }}
                  restore-keys: ubuntu-latest-node-

            - name: Restore docs
              id: docs_cache
              uses: actions/cache/restore@v3
              if: github.event_name != 'pull_request'
              with:
                  path: |
                      dist/apps/notes/docs
                  key: ubuntu-latest-docs-${{ hashFiles('apps/notes/docs/**/*') }}
                  restore-keys: ubuntu-latest-docs-

            - name: Deploy to GitHub Pages
              uses: peaceiris/actions-gh-pages@v3
              if: github.event_name != 'pull_request' && steps.docs_cache.outputs.cache-hit != 'true'
              with:
                  github_token: ${{ secrets.SSE_GITHUB_TOKEN }}
                  publish_dir: ./dist/apps/notes/docs
